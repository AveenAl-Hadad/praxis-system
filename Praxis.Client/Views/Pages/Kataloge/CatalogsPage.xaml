<UserControl x:Class="Praxis.Client.Views.Pages.Kataloge.CatalogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kataloge"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">

            <!-- Filter + Hauptaktionen -->
            <DockPanel Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <ComboBox x:Name="CatalogTypeBox"
                      Width="220"
                      SelectionChanged="CatalogTypeBox_SelectionChanged">
                        <ComboBoxItem Content="Diagnosen / ICD"/>
                        <ComboBoxItem Content="Leistungen / GOÄ / EBM"/>
                        <ComboBoxItem Content="Medikamente"/>
                        <ComboBoxItem Content="Formulare"/>
                        <ComboBoxItem Content="Dokumentvorlagen"/>
                    </ComboBox>

                    <StackPanel Margin="12,0,0,0">
                        <Grid Width="260" Margin="12,0,0,0">
                            <TextBox x:Name="SearchBox"
                                     Height="34"
                                     Padding="30,0,8,0"
                                     VerticalContentAlignment="Center"
                                     TextChanged="SearchBox_TextChanged"
                                     KeyDown="SearchBox_KeyDown"
                                     GotFocus="SearchBox_GotFocus"
                                     LostFocus="SearchBox_LostFocus"/>

                            <TextBlock Text="🔍"
                                       FontSize="14"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>

                            <TextBlock x:Name="SearchPlaceholder"
                                       Text="Suche..."
                                       Foreground="Gray"
                                       Margin="32,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>
                        </Grid>

                        <ListBox x:Name="SuggestionList"
                                 Width="260"
                                 MaxHeight="180"
                                 Visibility="Collapsed"
                                 MouseDoubleClick="SuggestionList_MouseDoubleClick"
                                 KeyDown="SuggestionList_KeyDown"
                                 DisplayMemberPath="DisplayText"/>
                    </StackPanel>
                    
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            DockPanel.Dock="Right"
                            HorizontalAlignment="Right">
                    
                    <Button x:Name="NewButton"
                            Content="Neu"
                            Width="90"
                            Margin="8,0,0,0"
                            Click="New_Click"/>

                    <Button x:Name="SaveButton"
                            Content="Speichern"
                            Width="100"
                            Margin="8,0,0,0"
                            Click="Save_Click"/>

                    <Button x:Name="DeleteButton"
                            Content="Löschen"
                            Width="90"
                            Margin="8,0,0,0"
                            Click="Delete_Click"/>
                </StackPanel>
            </DockPanel>

            <!-- Import-Bereich -->
            <Border x:Name="ImportPanel"
                    Background="#EEF3F6"
                    BorderBrush="#CCD6DD"
                    BorderThickness="1"
                    Padding="10"
                    Margin="0,0,0,10">
                <DockPanel>
                    <TextBlock Text="Import"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="0,0,16,0"/>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="ICD importieren"
                        Width="130"
                        Margin="0,0,8,0"
                        Click="ImportIcd_Click"/>

                        <Button Content="Medikamente importieren"
                        Width="170"
                        Margin="0,0,8,0"
                        Click="ImportMed_Click"/>

                        <Button Content="Leistungen importieren"
                        Width="160"
                        Click="ImportServices_Click"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Eingabeformular -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="CodeBox"
                 Grid.Column="0"
                 Margin="0,0,8,0"
                 VerticalContentAlignment="Center"/>

                <TextBox x:Name="NameBox"
                 Grid.Column="1"
                 Margin="0,0,8,0"
                 VerticalContentAlignment="Center"/>

                <TextBox x:Name="DescriptionBox"
                 Grid.Column="2"
                 VerticalContentAlignment="Center"/>
            </Grid>
        </StackPanel>
        <DataGrid x:Name="CatalogGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="CatalogGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="120"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="220"/>
                <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>