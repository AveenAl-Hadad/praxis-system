<Window x:Class="Praxis.Client.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Passwort ändern"
        Height="260"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Altes Passwort" Margin="0,0,0,5"/>
        <PasswordBox x:Name="OldPasswordBox" Grid.Row="1" Height="30" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Neues Passwort" Margin="0,0,0,5"/>
        <PasswordBox x:Name="NewPasswordBox" Grid.Row="3" Height="30" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Neues Passwort wiederholen" Margin="0,0,0,5"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="5" Height="30" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,20,0,0">
            <Button Content="Abbrechen"
                    Width="100"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>

            <Button Content="Speichern"
                    Width="100"
                    Height="30"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>